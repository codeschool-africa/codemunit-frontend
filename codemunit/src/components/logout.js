import React from 'react';
import PropTypes from 'prop-types';


const logout = () => {
    return (
        <div>
            
        </div>
    );
};


logout.propTypes = {
    
};


export default logout;
